import classNames from "classnames";
import styles from "./index.css";

export default function Hero() {
    return <div className={classNames(styles.DEFAULT_CLASS, styles.EXTRA_CLASS)}>

    </div>
};